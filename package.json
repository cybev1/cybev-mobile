{
  "name": "cybev-mobile",
  "version": "1.1.0",
  "description": "CYBEV Mobile - Enhanced with Push Notifications, Deep Linking & More",
  "main": "index.js",
  "scripts": {
    "cap:sync": "npx cap sync",
    "cap:sync:android": "npx cap sync android",
    "cap:sync:ios": "npx cap sync ios",
    "cap:open:android": "npx cap open android",
    "cap:open:ios": "npx cap open ios",
    "android:build:debug": "cd android && ./gradlew assembleDebug --no-daemon",
    "android:build:release": "cd android && ./gradlew assembleRelease --no-daemon",
    "android:bundle:release": "cd android && ./gradlew bundleRelease --no-daemon",
    "ios:build": "cd ios && xcodebuild -workspace App.xcworkspace -scheme App build",
    "postinstall": "npx cap sync",
    "test": "node -e \"console.log('ok')\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cybev1/cybev-mobile.git"
  },
  "keywords": [
    "cybev",
    "mobile",
    "capacitor",
    "social",
    "streaming"
  ],
  "author": "CYBEV",
  "license": "MIT",
  "type": "commonjs",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/cli": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {}
}
